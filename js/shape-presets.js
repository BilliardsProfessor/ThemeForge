ThemeForge.shapePresets = {
    type: "shape",
    schemaVersion: 1,
    saveButton: null,
    listElement: null,
    baseline: null,
    presets: [],
    feedbackTimerId: null,

    builtInPresets: [
        {
            id: "shape-square",
            type: "shape",
            name: "Square",
            schemaVersion: 1,
            builtIn: true,
            includeInRandomization: true,
            values: {
                corners: {
                    scale: {
                        none: { value: 0, unit: "px" },
                        xs: { value: 0, unit: "px" },
                        sm: { value: 0, unit: "px" },
                        md: { value: 0, unit: "px" },
                        lg: { value: 0, unit: "px" },
                        xl: { value: 0, unit: "px" },
                        pill: { value: 0, unit: "px" },
                    },
                    mappings: {
                        surfaceRadius: {
                            value: 0,
                            unit: "px",
                            cornerShape: "round",
                        },
                        cardRadius: {
                            value: 0,
                            unit: "px",
                            cornerShape: "round",
                        },
                        buttonRadius: {
                            value: 0,
                            unit: "px",
                            cornerShape: "round",
                        },
                        inputRadius: {
                            value: 0,
                            unit: "px",
                            cornerShape: "round",
                        },
                        badgeRadius: {
                            value: 0,
                            unit: "px",
                            cornerShape: "round",
                        },
                        dialogRadius: {
                            value: 0,
                            unit: "px",
                            cornerShape: "round",
                        },
                    },
                },
                borders: {
                    scale: {
                        none: { value: 0, unit: "px" },
                        thin: { value: 1, unit: "px" },
                        md: { value: 2, unit: "px" },
                        thick: { value: 4, unit: "px" },
                    },
                    mappings: {
                        cardBorderWidth: { value: 1, unit: "px" },
                        buttonBorderWidth: { value: 1, unit: "px" },
                        inputBorderWidth: { value: 1, unit: "px" },
                        dividerWidth: { value: 1, unit: "px" },
                        focusRingWidth: { value: 2, unit: "px" },
                    },
                },
            },
        },
        {
            id: "shape-subtle",
            type: "shape",
            name: "Subtle",
            schemaVersion: 1,
            builtIn: true,
            includeInRandomization: true,
            values: {
                corners: {
                    scale: {
                        none: { value: 0, unit: "px" },
                        xs: { value: 1, unit: "px" },
                        sm: { value: 2, unit: "px" },
                        md: { value: 4, unit: "px" },
                        lg: { value: 6, unit: "px" },
                        xl: { value: 8, unit: "px" },
                        pill: { value: 999, unit: "px" },
                    },
                    mappings: {
                        surfaceRadius: {
                            value: 8,
                            unit: "px",
                            cornerShape: "round",
                        },
                        cardRadius: {
                            value: 4,
                            unit: "px",
                            cornerShape: "round",
                        },
                        buttonRadius: {
                            value: 4,
                            unit: "px",
                            cornerShape: "round",
                        },
                        inputRadius: {
                            value: 4,
                            unit: "px",
                            cornerShape: "round",
                        },
                        badgeRadius: {
                            value: 999,
                            unit: "px",
                            cornerShape: "round",
                        },
                        dialogRadius: {
                            value: 8,
                            unit: "px",
                            cornerShape: "round",
                        },
                    },
                },
                borders: {
                    scale: {
                        none: { value: 0, unit: "px" },
                        thin: { value: 1, unit: "px" },
                        md: { value: 2, unit: "px" },
                        thick: { value: 3, unit: "px" },
                    },
                    mappings: {
                        cardBorderWidth: { value: 1, unit: "px" },
                        buttonBorderWidth: { value: 1, unit: "px" },
                        inputBorderWidth: { value: 1, unit: "px" },
                        dividerWidth: { value: 1, unit: "px" },
                        focusRingWidth: { value: 2, unit: "px" },
                    },
                },
            },
        },
        {
            id: "shape-rounded",
            type: "shape",
            name: "Rounded",
            schemaVersion: 1,
            builtIn: true,
            includeInRandomization: true,
            values: {
                corners: {
                    scale: {
                        none: { value: 0, unit: "px" },
                        xs: { value: 2, unit: "px" },
                        sm: { value: 4, unit: "px" },
                        md: { value: 8, unit: "px" },
                        lg: { value: 12, unit: "px" },
                        xl: { value: 20, unit: "px" },
                        pill: { value: 999, unit: "px" },
                    },
                    mappings: {
                        surfaceRadius: {
                            value: 18,
                            unit: "px",
                            cornerShape: "round",
                        },
                        cardRadius: {
                            value: 12,
                            unit: "px",
                            cornerShape: "round",
                        },
                        buttonRadius: {
                            value: 8,
                            unit: "px",
                            cornerShape: "squircle",
                        },
                        inputRadius: {
                            value: 8,
                            unit: "px",
                            cornerShape: "round",
                        },
                        badgeRadius: {
                            value: 999,
                            unit: "px",
                            cornerShape: "round",
                        },
                        dialogRadius: {
                            value: 16,
                            unit: "px",
                            cornerShape: "round",
                        },
                    },
                },
                borders: {
                    scale: {
                        none: { value: 0, unit: "px" },
                        thin: { value: 1, unit: "px" },
                        md: { value: 2, unit: "px" },
                        thick: { value: 4, unit: "px" },
                    },
                    mappings: {
                        cardBorderWidth: { value: 1, unit: "px" },
                        buttonBorderWidth: { value: 1, unit: "px" },
                        inputBorderWidth: { value: 1, unit: "px" },
                        dividerWidth: { value: 1, unit: "px" },
                        focusRingWidth: { value: 2, unit: "px" },
                    },
                },
            },
        },
        {
            id: "shape-soft",
            type: "shape",
            name: "Soft",
            schemaVersion: 1,
            builtIn: true,
            includeInRandomization: true,
            values: {
                corners: {
                    scale: {
                        none: { value: 0, unit: "px" },
                        xs: { value: 4, unit: "px" },
                        sm: { value: 8, unit: "px" },
                        md: { value: 12, unit: "px" },
                        lg: { value: 18, unit: "px" },
                        xl: { value: 28, unit: "px" },
                        pill: { value: 999, unit: "px" },
                    },
                    mappings: {
                        surfaceRadius: {
                            value: 28,
                            unit: "px",
                            cornerShape: "squircle",
                        },
                        cardRadius: {
                            value: 18,
                            unit: "px",
                            cornerShape: "squircle",
                        },
                        buttonRadius: {
                            value: 999,
                            unit: "px",
                            cornerShape: "round",
                        },
                        inputRadius: {
                            value: 12,
                            unit: "px",
                            cornerShape: "squircle",
                        },
                        badgeRadius: {
                            value: 999,
                            unit: "px",
                            cornerShape: "round",
                        },
                        dialogRadius: {
                            value: 28,
                            unit: "px",
                            cornerShape: "squircle",
                        },
                    },
                },
                borders: {
                    scale: {
                        none: { value: 0, unit: "px" },
                        thin: { value: 1, unit: "px" },
                        md: { value: 2, unit: "px" },
                        thick: { value: 4, unit: "px" },
                    },
                    mappings: {
                        cardBorderWidth: { value: 1, unit: "px" },
                        buttonBorderWidth: { value: 0, unit: "px" },
                        inputBorderWidth: { value: 1, unit: "px" },
                        dividerWidth: { value: 1, unit: "px" },
                        focusRingWidth: { value: 3, unit: "px" },
                    },
                },
            },
        },
    ],

    async init() {
        this.saveButton = document.querySelector("[data-save-shape-preset]");
        this.listElement = document.querySelector("[data-shape-preset-list]");

        if (this.saveButton) {
            this.saveButton.addEventListener("click", (event) => {
                event.preventDefault();
                event.stopPropagation();

                this.promptToSave();
            });
        }

        if (this.listElement) {
            this.listElement.addEventListener("click", (event) => {
                const button = event.target.closest("[data-shape-preset-id]");

                if (!button) {
                    return;
                }

                this.applyPreset(button.dataset.shapePresetId);
            });
        }

        this.setBaseline();
        await this.loadPresets();
    },

    async loadPresets() {
        try {
            const savedPresets = await ThemeForge.storage.getPresets(this.type);

            this.presets = [...this.builtInPresets, ...savedPresets];
            this.render();
        } catch (error) {
            console.error("Theme Forge could not load Shape presets.", error);

            this.presets = [...this.builtInPresets];
            this.render();
        }
    },

    render() {
        if (!this.listElement) {
            return;
        }

        const builtInPresets = this.presets.filter((preset) => preset.builtIn);
        const savedPresets = this.presets.filter((preset) => !preset.builtIn);

        this.listElement.replaceChildren(
            this.createPresetGroup("Built-in", builtInPresets),
            this.createPresetGroup("Saved", savedPresets),
        );
    },

    createPresetGroup(label, presets) {
        const group = document.createElement("section");
        const heading = document.createElement("h4");
        const list = document.createElement("div");

        group.className = "workbench-preset-group";
        heading.className = "workbench-preset-group-title";
        heading.textContent = label;
        list.className = "workbench-preset-list";

        if (!presets.length) {
            const emptyMessage = document.createElement("p");

            emptyMessage.className = "workbench-preset-empty";
            emptyMessage.textContent = "No saved presets yet.";
            list.append(emptyMessage);
        } else {
            presets.forEach((preset) => {
                list.append(this.createPresetButton(preset));
            });
        }

        group.append(heading, list);

        return group;
    },

    createPresetButton(preset) {
        const button = document.createElement("button");
        const name = document.createElement("span");
        const source = document.createElement("span");

        button.type = "button";
        button.className = "workbench-preset-button";
        button.dataset.shapePresetId = preset.id;
        button.setAttribute("aria-pressed", String(this.matchesCurrentShape(preset)));

        name.className = "workbench-preset-name";
        name.textContent = preset.name;

        source.className = "workbench-preset-source";
        source.textContent = preset.builtIn ? "Built-in" : "Saved";

        button.append(name, source);

        return button;
    },

    applyPreset(id) {
        const preset = this.presets.find((item) => item.id === id);

        if (!preset || this.matchesCurrentShape(preset)) {
            return;
        }

        const previousPreset = this.presets.find((item) => this.matchesCurrentShape(item));

        ThemeForge.history.recordChange("Shape Preset");

        ThemeForge.history.updateLatestChangeDetail({
            type: "value",
            label: "Shape Preset",
            // before: previousPreset?.name || "Custom",
            after: preset.name,
        });

        ThemeForge.theme.shape = this.cloneShapeValues(preset.values);

        ThemeForge.refreshThemeInterface();
        ThemeForge.history.saveSession();

        this.setBaseline();
        this.render();
    },

    matchesCurrentShape(preset) {
        return JSON.stringify(preset.values) === JSON.stringify(ThemeForge.theme.shape);
    },

    async promptToSave() {
        if (!ThemeForge.storage || !ThemeForge.appModal) {
            this.showErrorFeedback("Shape preset saving is unavailable");
            return;
        }

        try {
            const suggestedName = await this.getSuggestedName();

            const enteredName = await ThemeForge.appModal.prompt({
                eyebrow: "Shape Preset",
                title: "Save Shape Preset",
                label: "Preset name",
                value: suggestedName,
                confirmText: "Save Preset",
                cancelText: "Cancel",
            });

            if (enteredName === null) {
                return;
            }

            const name = enteredName.trim() || suggestedName;

            await this.save(name);
            this.setBaseline();
            await this.loadPresets();
        } catch (error) {
            console.error("Theme Forge could not save the Shape preset.", error);

            this.showErrorFeedback("Shape preset could not be saved");
        }
    },

    async getSuggestedName() {
        const presets = await ThemeForge.storage.getPresets(this.type);

        return `Custom Shape ${presets.length + 1}`;
    },

    save(name) {
        return ThemeForge.storage.savePreset({
            type: this.type,
            name,
            schemaVersion: this.schemaVersion,
            builtIn: false,
            includeInRandomization: true,
            values: this.cloneShapeValues(ThemeForge.theme.shape),
        });
    },

    setBaseline(shape = ThemeForge.theme.shape) {
        this.baseline = this.cloneShapeValues(shape);
        this.updateSaveButtonVisibility();
    },

    isDirty() {
        if (!this.baseline) {
            return false;
        }

        return JSON.stringify(this.baseline) !== JSON.stringify(ThemeForge.theme.shape);
    },

    updateSaveButtonVisibility() {
        if (!this.saveButton) {
            return;
        }

        this.saveButton.hidden = !this.isDirty();
    },

    cloneShapeValues(shape) {
        if (typeof structuredClone === "function") {
            return structuredClone(shape);
        }

        return JSON.parse(JSON.stringify(shape));
    },

    showErrorFeedback(message) {
        if (!this.saveButton) {
            return;
        }

        window.clearTimeout(this.feedbackTimerId);

        this.saveButton.dataset.saveState = "error";
        this.saveButton.dataset.tooltip = message;
        this.saveButton.setAttribute("aria-label", message);

        this.feedbackTimerId = window.setTimeout(() => {
            delete this.saveButton.dataset.saveState;
            this.saveButton.dataset.tooltip = "Save Shape preset";
            this.saveButton.setAttribute("aria-label", "Save Shape preset");
        }, 1800);
    },
};

document.addEventListener("DOMContentLoaded", () => {
    ThemeForge.shapePresets.init();
});
